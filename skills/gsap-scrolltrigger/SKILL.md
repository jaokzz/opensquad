---
name: gsap-scrolltrigger
description: "GSAP (GreenSock) com todos os plugins premium gratuitos — ScrollTrigger, SplitText, MorphSVG, DrawSVG, Flip, MotionPath e técnicas avançadas de animação para landing pages e sites de marketing."
type: prompt
version: "1.0.0"
categories: [frontend, animation, gsap]
---

Ao precisar de animação orientada a scroll, pin de seção, timelines complexas ou text/SVG morphing, invoque a skill global `gsap-scrolltrigger` (via Skill tool) para as diretrizes completas de implementação — ScrollTrigger (pin/scrub/markers), SplitText, MorphSVG, DrawSVG, Flip e MotionPath. Sempre respeitar as regras de performance do squad: animar apenas `transform`/`opacity`, cleanup de `gsap.context().revert()` em todo `useEffect`, nunca misturar com Framer Motion no mesmo componente.
